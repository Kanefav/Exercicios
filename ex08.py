print('---- Calculadora ----')
numI = (input('valor inicial: '))
numI2 = (input('valor que vai dividir o inicial:'))
numII = numI.values(str(','))
if numI and numI2 == float(numI and numI2):
    numI and numI2.replace(',','.')
    xbeta = numI2 * numI
    print(f'o valor da divisão é {xbeta:.1f}')
if numI and numI2 == str(numI and numI2):
   xbeta2 = numI * numI2
   print(f'')
